numpy>=1.26
scipy>=1.11
pandas>=2.0
pytest>=7.4

# Optional: production-grade drop-in replacement for src/als.py.
# implicit's ImplicitALS is written in Cython and multithreaded, so it's
# ~10-50x faster on large datasets. This project's ALS is implemented from
# scratch instead, to demonstrate the underlying algorithm -- see README.
# implicit>=0.7
